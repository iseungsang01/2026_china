# 1안 + 화산 기준 삭제 계획

작성일: 2026-07-30
기준안: `1V` - `1안 + 화산`

> **2026-07-31 실행 완료.** P0~P2를 전부 실행했다 — 기본값 1V 고정(`79d0234`),
> 인솔서 1V 재작성(`4699b93`), 비교 UI·대안 데이터 삭제(`5727e29`).
> **P3(검증기의 1A·2A·2B 플랜 축소)만 계획대로 보류** — 1V 채택 근거의 비교 자료로
> `verify_itinerary.py`에 남겨 두었고, 페이지 TT는 `gen_timetable.PAGE_PLANS`로 1V만 싣는다.
> 아래 「삭제 후 검색 기준」은 화면 코드에서 통과했다(허용 잔존: 제외 사유 한 줄 수준의
> 자금성·이화원 언급, base64 타일 안의 우연한 문자열).

이 문서는 실제 삭제 실행 전에 기준선을 고정하기 위한 작업 문서다. 이번 단계에서는 파일을 지우지 않고, `1V` 기준으로 남길 것과 먼저 삭제할 것을 정리한다.

## 확정 기준

`1V`는 `1안`의 8/11 후허하오터 반나절을 우란하다 화산으로 바꾸는 구성이다. 사막 밤과 베이징 귀경 구조는 1안 계열을 유지한다.

| 날짜 | 유지할 핵심 |
| --- | --- |
| 8/10 | PEK T3 도착, 첸먼 숙박, 천단 17:05, 베이징덕, 첸먼다제/다잔란 |
| 8/11 | 베이징에서 우란차부 이동, 우란하다 화산 14:25-16:55, 후허하오터 구시가 1박, 사이상라오제 야식 |
| 8/12 | 후허하오터 병합단 집합, 샹사완 경구 입장, 사막 일몰, 페르세우스 유성우 극대기 |
| 8/13 | 사막에서 `离团`, 바오터우역, D1068로 베이징 복귀, 천안문 광장, 첸먼 저녁, 공항 인근 숙박 |
| 8/14 | 08:00 PEK T3 도착 목표, 10:40 출국 |

현재 코드 기준으로 `1V` 원천은 [tools/verify_itinerary.py](tools/verify_itinerary.py)의 `build(1, "volc")`이며, 시간표 생성 키는 `1V`다.

## 현재 불일치

| 위치 | 현재 상태 | 1V 기준으로 바꿀 방향 |
| --- | --- | --- |
| [index.html](index.html) | 사이드바에 `1A`, `1V`, `2A`, `2B`가 모두 있고 기본값은 `ORDER="2B"` | 기본값을 `1V`로 바꾸고 대안 선택 UI 삭제 |
| [index.html](index.html) | `일정 비교`, 비용 비교, 예약 매트릭스가 4개 안을 나란히 보여 줌 | `1V` 단일 일정의 실행 정보만 남김 |
| [guide.html](guide.html) | 제목/본문이 `4안 인솔서` | `1V` 인솔서로 재작성하거나 임시로 링크 제거 |
| [guide-data.js](guide-data.js) | `4안 · 유네스코` 본문, 8/11 사막, 8/13 자금성/이화원 기준 | 8/11 화산, 8/12 사막, 8/13 천안문 광장 기준으로 재작성 |
| [tools/gen_timetable.py](tools/gen_timetable.py) | `GUIDE_PLAN = None`이라 인솔서 시간표 동결 | `GUIDE_PLAN = "1V"`로 풀고 guide 시간표 재생성 |
| [tools/verify_itinerary.py](tools/verify_itinerary.py) | `PLANS`에 `1A`, `1V`, `2A`, `2B` 모두 유지 | 검증 안정화 뒤 `1V` 단일 플랜으로 축소 가능 |

## 삭제 원칙

1. 먼저 사용자에게 보이는 잘못된 정보부터 제거한다.
2. `1V` 시간표를 만드는 원천은 삭제하지 않는다.
3. 비교용 데이터는 `1V` 단일 화면이 정상 동작한 뒤 삭제한다.
4. 구 `4안` 인솔서 문구는 그대로 두면 실제 여행 의사결정에 해가 되므로 최우선 삭제/교체 대상이다.
5. 일정 검증 스크립트는 마지막에 줄인다. UI보다 먼저 줄이면 회귀 비교 근거가 사라진다.

## 1차 삭제 대상

| 우선순위 | 대상 | 삭제 이유 | 선행 조건 |
| --- | --- | --- | --- |
| P0 | `guide.html`의 `4안 인솔서` 제목/메타/헤더 | 확정안과 직접 충돌 | `GUIDE_PLAN="1V"` 설정 또는 인솔서 링크 임시 제거 |
| P0 | `guide-data.js`의 `4안 · 유네스코` 개요와 8/13 자금성/이화원 본문 | `1V`에는 자금성/이화원 내부 관람이 없음 | `1V` 일자별 본문 초안 작성 |
| P0 | `index.html` 기본 선택 `2B` | 첫 화면이 확정안과 다름 | `ORDER="1V"`, `ob-1V`만 active |
| P1 | 사이드바의 `1A`, `2A`, `2B` 버튼 | 결정 이후 비교 조작 불필요 | `1V` 단일 시간표 렌더링 확인 |
| P1 | `일정 비교` 패널 | 결정 이후 의사결정용 표 불필요 | 비용/예약/정책 패널이 `1V` 단일 기준으로 정리된 뒤 |
| P1 | 비용 표의 4안 비교 컬럼 | `1V` 금액만 필요 | `1V` 총액/추정값 재계산 |
| P1 | 예약 난이도의 구성별 오픈 시각 4열 표 | `1V` 예약 달력만 필요 | `1V` 예약 리스트 별도 정리 |
| P2 | `POI()` 내부의 `2A`, `2B`, `1A` 조건 분기 | 화면 코드 복잡도와 오류 가능성 증가 | 지도/카드가 `1V` 단일로 동작 확인 |
| P2 | `NIGHTS`, `SPOT.when`의 대안별 값 | `1V` 외 밤 정보 불필요 | 지도 보조 라벨 단일값으로 교체 |
| P2 | 무톈위/바다링 옵션 상세 | 이번 확정안에서 실행하지 않음 | 부록으로도 남길 필요 없다는 판단 |
| P3 | `verify_itinerary.py`의 `1A`, `2A`, `2B` 플랜 | 장기 유지 비용 감소 | `1V` 검증과 생성이 모두 통과한 뒤 |

## 남길 것

| 범주 | 남길 내용 |
| --- | --- |
| 시간표 | `TT["1V"]` 또는 이를 단일 `TT`로 변환한 데이터 |
| 지도 | 베이징, 후허하오터, 우란하다 화산군, 샹사완, 바오터우, PEK |
| 베이징 | 첸먼, 천단, 천안문 광장, 공항 이동 |
| 내몽고 | 우란차부/화산 직통버스, 후허하오터 구시가, 샹사완 경구, 바오터우 복귀 |
| 예약 | 12306, 사막 경구 내 텐트/패키지, 천단, 천안문 광장, 우란하다 화산 |
| 리스크 | D1077 시발역, 화산 직통버스와 D2005 연결, D1068 지연, 8/12 사막 기상 |
| 공통 실무 | 무비자, 결제, 여권 원본, 통신/VPN, 날씨/복장, 보험 주의 |

## 삭제 보류 대상

| 대상 | 보류 이유 |
| --- | --- |
| `tools/verify_itinerary.py`의 대안 플랜 | 삭제 전 `1V`가 왜 채택됐는지 비교 근거로 한 번 더 필요 |
| `index.html`의 지도 기반 공통 함수 | 옵션 분기와 무관한 렌더링/핀/줌 코드 |
| `check_guide.py` | `1V` 인솔서 재작성 뒤 라벨 누락 검증에 필요 |
| 구 `guide-data.js`의 공통 실무 문단 | 무비자, 결제, 공항, 날씨, 전화번호 등은 `1V`에도 재사용 가능 |

## 구현 순서

1. `index.html` 첫 화면을 `1V`로 고정한다.
   - `ORDER="2B"`를 `ORDER="1V"`로 변경
   - `ob-2B`의 `on` 클래스 제거, `ob-1V`에 적용
   - 이 단계에서는 다른 버튼을 아직 삭제하지 않고 기본값만 고정

2. `guide.html`이 잘못된 정보를 보여 주지 않게 한다.
   - 빠른 안전 조치: `index.html`의 인솔서 링크를 임시 숨김 또는 "정리 중" 처리
   - 정식 조치: `tools/gen_timetable.py`의 `GUIDE_PLAN = "1V"` 설정 후 `guide.html` 시간표 재생성
   - 이후 `guide-data.js`를 `1V` 일자별 본문으로 교체

3. 화면에서 대안 비교를 삭제한다.
   - 사이드바의 `1A`, `2A`, `2B` 버튼 삭제
   - `일정 비교` 패널 삭제
   - 비용/예약/정책/날씨 표를 `1V` 단일 열로 축소

4. 지도 카드의 조건 분기를 단일화한다.
   - `POI()`의 `ONE`, `TWO`, `o==="..."` 분기를 `1V` 문구로 평탄화
   - `NIGHTS`와 `SPOT.when`을 문자열 단일값으로 변경
   - `1V`에서 쓰지 않는 관광지 카드는 대체 후보가 아니면 삭제

5. 생성기와 검증기를 줄인다.
   - `PLANS`를 `1V` 하나로 축소
   - `build_tt()`가 단일 시간표를 만들도록 정리
   - `check_guide.py`가 `1V` guide만 검사하도록 정리

## 삭제 후 검색 기준

삭제가 끝난 뒤 아래 검색어가 일반 화면 코드에서 사라져야 한다. 단, `cleanup-plan-1v.md` 같은 작업 문서에는 남아도 된다.

```powershell
rg -n "4안|2안-A|2안-B|2B|추천|프라이빗단|자금성.*이화원|이화원 연표|무톈위|바다링|초원 \\(제외\\)" .
```

허용되는 잔존 표현:

| 표현 | 허용 위치 |
| --- | --- |
| `1안 + 화산`, `1V` | 확정안 표시와 코드 키 |
| `자금성`, `이화원` | "이번 확정안에서는 제외" 수준의 짧은 설명만 |
| `바다링`, `무톈위` | 삭제 계획 문서나 별도 보관 문서 |
| `초원` | 제외 사유 한 줄 또는 삭제 계획 문서 |

## 검증 기준

삭제 작업을 실제 실행한 뒤 다음을 통과해야 한다.

```powershell
python -m py_compile tools\verify_itinerary.py tools\gen_timetable.py tools\check_guide.py
python tools\gen_timetable.py --write
python tools\check_guide.py
```

수동 확인:

1. `index.html` 첫 화면이 `1안 + 화산`으로 열린다.
2. 8/11 지도/카드가 우란하다 화산을 보여 준다.
3. 8/12 밤이 샹사완 사막과 유성우 극대기로 유지된다.
4. 8/13이 자금성/이화원이 아니라 천안문 광장으로 표시된다.
5. `guide.html`에 `4안`, `유네스코 둘`, `자금성 → 이화원` 같은 구 일정 문구가 남지 않는다.

## 다음 작업 단위

가장 안전한 첫 PR/커밋 범위는 "표시 기준 고정"이다.

1. `index.html` 기본값만 `1V`로 변경
2. `guide.html` 링크를 임시 숨김 또는 안내 문구로 변경
3. `cleanup-plan-1v.md` 유지

그 다음 커밋에서 인솔서 재작성과 비교 UI 삭제를 나누는 것이 좋다. 인솔서는 라벨 매칭 실패가 나기 쉬워 `check_guide.py` 통과를 별도 경계로 두는 편이 안전하다.
